import React from 'react';
import  ReactDOM  from 'react-dom';
import { Form, ExampleComponent } from './App';

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
      <Form/>
      <ExampleComponent/>
  </React.StrictMode>
);

